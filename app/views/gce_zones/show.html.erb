<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @gce_zone.name %>
</p>

<p>
  <strong>Is active:</strong>
  <%= @gce_zone.is_active %>
</p>

<p>
  <strong>GCE Region:</strong>
  <%= link_to @gce_zone.gce_region, @gce_zone.gce_region %> 
</p>

<h2>Normal Machine Types</h2>

<% @gce_zone.machine_types.each do |mt| %>
  <p> <%= link_to mt,mt %> </p>
<% end %>

<%= link_to 'Edit', edit_gce_zone_path(@gce_zone) %> |
<%= link_to 'Back', gce_zones_path %>
